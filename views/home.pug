extends layouts/main.pug
include mixins/videoBlock

block content
    .videos
        each item in videos
            //- h1= item.title
            //- p= item.description
            +videoBlock({
                title: item.title,
                views: item.views,
                videoFile: item.videoFile
            })